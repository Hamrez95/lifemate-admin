# LifeMate Command Center

Internal management frontend for the LifeMate ecosystem.

> Security boundary: the browser must never query or mutate sensitive production healthcare tables directly. Administrative data access is mediated by the authenticated `lifemate-admin-api` server boundary.

The application is implemented incrementally through reviewed pull requests. See `AGENTS.md` and `SECURITY.md` for engineering and security rules.
